<!DOCTYPE html>
<html lang="en-GB">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ renderData.title or title or metadata.title }} — Paavan's Blog</title>
  <meta property="og:title" content="{{ renderData.title or title or metadata.title }}">
  <meta property="og:description" content="{{ renderData.description or description or metadata.description }}">
  <meta name="twitter:card" content="summary_large_image">
  <link rel="stylesheet" href="{{ '/css/index.css' | url }}">
  <!-- <link rel="stylesheet" href="{{ '/css/prism-base16-monokai.dark.css' | url }}"> -->
  <link rel="alternate" href="{{ metadata.feed.path | url }}" type="application/atom+xml" title="{{ metadata.title }}">
  <link href="https://fonts.googleapis.com/css2?family=EB+Garamond:ital,wght@0,400;0,600;1,400&display=swap" rel="stylesheet">
  <link rel="icon" type="image/png" href="/img/favicon.png">
  {%- if canonical -%}
  <link rel="canonical" href="{{ canonical }}">
  {%- endif -%}
  {%- if socialimage -%}
  <meta property="og:image" content="{{ socialimage }}">
  {%- endif -%}
</head>
<body>
  <div class="page">
  <header>
    <div class="header-inner">
    <h1 class="home"><a href="{{ '/' | url }}">paavan/blog</a></h1>

    {#- Read more about `eleventy-navigation` at https://www.11ty.dev/docs/plugins/navigation/ #}
    <ul class="nav">
      {%- for entry in collections.all | eleventyNavigation %}
      <li class="nav-item{% if entry.url == page.url %} nav-item-active{% endif %}"><a href="{{ entry.url | url }}">{{ entry.title }}</a></li>
      {%- endfor %}
    </ul>
  </div>
  </header>
  <div class="container">
    <main{% if templateClass %} class="{{ templateClass }}"{% endif %}>
      {{ content | safe }}
    </main>


{%- if footer -%} 

<footer>
 
  <h3>Read more...</h3>

  <p>If you liked this, check out some of the most popular posts.</p>
  <ul>
    <li><a href="/posts/the-loss-of-certainty/">The loss of certainty</a> <time class="postlist-date" datetime="2020-05-28">28 May 2020</time></li>
    <li><a href="/posts/these-are-my-favourite-personal-websites/">These are my favourite personal websites</a> <time class="postlist-date" datetime="2020-05-14">14 May 2020</time></li>
    <li><a href="/posts/hills-the-best-framework-for-determining-where-to-go/">Hills: the best framework for determining where to go</a> <time class="postlist-date" datetime="2019-11-25">25 Nov 2019</time></li>
  </ul>


  <p><a href="{{ '/' | url }}">← Home</a></p>

</footer>
</div>

{%- endif -%}

</body>

<script>

// Find header
let header = document.getElementsByTagName('header');

// Add class on page load if already scrolled
if (window.pageYOffset > 40) {
    header[0].classList.add('active');
}

// Update class when scolling
window.onscroll = function() {
    if (window.pageYOffset > 40) {
        header[0].classList.add('active');
    } else {
        header[0].classList.remove('active');
    }
}
</script>

</html>